CREATE TABLE accounting (
    date DATE NOT NULL,
    invoice INT NOT NULL,
    description VARCHAR(90) NULL,
    detail VARCHAR(60) NOT NULL,
    quantity INT NULL,
    income INT NULL,
    expenses INT NULL,
    total INT NULL
);